#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    // double x;
    // scanf("%lf", &x);
    // int ans = ceil(x);
    // printf("%d", ans);

    // double x;
    // scanf("%lf", &x);
    // int ans = floor(x);
    // printf("%d", ans);

    // double x;
    // scanf("%lf", &x);
    // int ans = round(x);
    // printf("%d", ans);

    // int x;
    // scanf("%d", &x);
    // int ans = sqrt(x);
    // printf("%d", ans);

    // int a, b;
    // scanf("%d %d", &a, &b);
    // double ans = pow(a, b);
    // printf("%d", (int) ans);

    int x;
    scanf("%d", &x);
    int ans = abs(x);
    printf("%d", ans);
    return 0;
}