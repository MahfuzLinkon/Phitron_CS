#include <stdio.h>
int main()
{
    int ar[] = {1, 2, 3};
    printf("%d", ar[3]);
    // int ar[5] = {1}; // initialize one value other will be zero

    // int ar[5] = {0}; // initialize one value with all value will be zero

    // for (int i = 0; i < 5; i++)
    // {
    //     printf("%d\n", ar[i]);
    // }
    
    return 0;
}