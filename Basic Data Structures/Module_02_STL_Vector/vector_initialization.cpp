#include <bits/stdc++.h>

using namespace std;

int main()
{
    // vector<int>v;

    // vector<int> v(5);

    // vector<int> v(5, 100);

    // vector<int>v2(5,10);
    // vector<int> v(5, 100);

    // int ar[5] = {10,20,30,40,50};
    // vector<int>v(ar, ar+5);

    vector<int>v = {1,2,3};

    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << " ";
    }

    cout << endl;
    cout << v.size() << endl;

    return 0;
}